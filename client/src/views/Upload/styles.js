import background from 'static/phone.jpg'
export const styles = theme=>({
    root:{
        margin:'5em',
        height:500,
        backgroundg:`url(${background}`
    },
    card:{
        padding:theme.spacing.unit*3
    }
})